"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.extractBlock = extractBlock;
exports.parseBlock = parseBlock;
exports.parseLine = parseLine;

var _extractComments = require("extract-comments");

var _extractComments2 = _interopRequireDefault(_extractComments);

var _types = require("./types");

var _syntax = require("./syntax");

var _common = require("./common");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const REGEX_START_TAG = new RegExp(_common.r`^\s*` + _syntax.START_TAG + _common.r`\s*$`);
const REGEX_END_TAG = new RegExp(_common.r`^\s*` + _syntax.END_TAG + _common.r`\s*$`);
function extractBlock(userscript) {
    const lineComments = (0, _extractComments2.default)(userscript).filter(c => c.type === "LineComment").map(c => c.raw);
    if (lineComments.length === 0) {
        return (0, _types.Left)("NO_LINE_COMMENTS" /* NO_LINE_COMMENTS */);
    } else {
        const startTagIndex = lineComments.findIndex(x => REGEX_START_TAG.test(x));
        const endTagIndex = lineComments.findIndex(x => REGEX_END_TAG.test(x));
        if (startTagIndex < 0) {
            return (0, _types.Left)("NO_START_TAG" /* NO_START_TAG */);
        } else if (endTagIndex < 0) {
            return (0, _types.Left)("NO_END_TAG" /* NO_END_TAG */);
        } else if (endTagIndex < startTagIndex) {
            return (0, _types.Left)("END_TAG_BEFORE_START_TAG" /* END_TAG_BEFORE_START_TAG */);
        } else {
            return (0, _types.Right)(lineComments.slice(startTagIndex + 1, endTagIndex).join("\n"));
        }
    }
}
function parseBlock(block) {
    const lines = block.split("\n").filter(line => /\S/.test(line));
    const validEntries = [];
    const invalidLines = [];
    lines.map(parseLine).forEach(result => {
        if ((0, _types.isLeft)(result)) {
            invalidLines.push(result.Left);
        } else {
            validEntries.push(result.Right);
        }
    });
    return invalidLines.length > 0 ? (0, _types.Left)(invalidLines) : (0, _types.Right)(validEntries);
}
// `line` is expected to be a line without any comment prefix, but leading whitespace is OK.
function parseLine(line) {
    const match = line.match(new RegExp(_common.r`^\s*` + _syntax.TAG_PREFIX + _common.r`([a-z\-]+)` + _common.r`(?:\s*|\s+(\S.*))$`, "i"));
    if (match === null) {
        return (0, _types.Left)(line);
    } else {
        const key = match[1];
        const value = match[2];
        return (0, _types.Right)({ key, value: value ? value.trimEnd() : true });
    }
}