"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.validConnectValueRequired = undefined;
exports.isValidConnectValue = isValidConnectValue;

var _isIp = require("is-ip");

var _isIp2 = _interopRequireDefault(_isIp);

var _isValidDomain = require("is-valid-domain");

var _isValidDomain2 = _interopRequireDefault(_isValidDomain);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/*
> <value> can have the following values:
>
>   • domains like "tampermonkey.net" (this will also allow all sub-domains)
>   • sub-domains i.e. "safari.tampermonkey.net"
>   • "self" to whitelist the domain the script is currently running at
>   • "localhost" to access the localhost
>   • "1.2.3.4" to connect to an IP address
>   • "*"
>
👉 https://www.tampermonkey.net/documentation.php#_connect
*/
const CONNECT_KEYWORDS = ["self", "localhost", "*"];
function isValidConnectValue(v) {
    return CONNECT_KEYWORDS.includes(v) || (0, _isValidDomain2.default)(v) || (0, _isIp2.default)(v);
}
const validConnectValueRequired = exports.validConnectValueRequired = `Value must be a domain (e.g. example.com), an IP address (e.g. 1.2.3.4), or one of ${CONNECT_KEYWORDS.map(x => `"${x}"`).join(", ")}.`;